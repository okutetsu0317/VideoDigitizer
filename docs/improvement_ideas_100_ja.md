# VideoDigitizer 改善案100

対象：2026年9月20日時点のWeb版を中心としたコード・画面・ガイド。

## 方針

- 実装案の一覧です。未実装の断定や正式な不具合報告ではありません。既存機能の拡張・検証強化も含みます。着手時には該当実装を再確認します。
- 既存の4点法、ズーム、補間、欠測チェック、再測定比較、Undo/Redo、操作履歴、診断情報、自動保存は新機能として数え直していません。
- QAチャット・生成LLMは再導入しません。動画・画像のクラウド保存も前提にしません。
- 100件すべてを追加する方針ではありません。軽さ・測定の信頼性・操作の分かりやすさを改善しない案は見送ります。
- 統計・測定手法の案は検証課題を含み、妥当性が確認済みという意味ではありません。

## 優先度

- A：先に評価する候補。データ保護・測定の整合性・重さの対策を重視。
- B：基本動作が安定した後、作業時間や誤操作の実測で選ぶ候補。
- C：対象利用者と用途がはっきりしたら検討する候補。

## 最初の10件

推奨する検討順：#11 → #2 → #12 → #14 → #17 → #43 → #73 → #74 → #81 → #57。
まず速度を測り、打点の整合性と重さ・保存の安全性を改善。その後に案内と研究用比較の改善へ進みます。番号は下の一覧と対応します。

実装状況（2026年9月20日）：#11、#2、#12、#14、#17、#43、#57、#73、#74、#81の第1段階を完了しました。実動画と遅延・失敗注入を含む回帰試験を継続します。

## 確認した実装

- `web_viewer/index.html`：現在の操作・分析・設定項目。
- `web_viewer/app.js`：フレーム移動、キャッシュ、較正、品質チェック、再測定比較、保存、診断等。
- `docs/user_guide_ja.md` と `web_viewer/help.html`：既存の利用手順。

## フレーム・座標の信頼性

### 1. 表示フレームの照合状態を分かりやすくする［A］

既存の時刻照合を、確認済み・確認不能・処理中で区別して表示する。 完了の目安：照合不能な画像に打点できず、停止理由を読める。

### 2. フレーム移動と打点の整合性テストを拡充する［A・第1段階完了］

連打、移動失敗、途中キャンセルでも別フレームに座標が付かないことを自動検証する。 完了の目安：遅延・失敗を注入した試験で画像と保存先フレームが一致する。

### 3. 撮影時刻と再生時刻を明確に分ける［A］

既存のFPS・時刻CSVを活かし、スロー動画のどの時間軸で分析するか明示する。 完了の目安：表示・速度計算・出力で使用した時間軸を追跡できる。

### 4. 動画読込前の適合診断を具体化する［B］

コンテナ、コーデック、時刻情報、ブラウザの対応状況を分けて案内する。 完了の目安：開けない理由と安全な次の操作が分かる。

### 5. 回転・縦動画の座標系テストを増やす［A］

回転メタデータ、表示回転、元ピクセル座標の対応を固定する。 完了の目安：90度回転や縦動画でも保存・再読込後の点が一致する。

### 6. 特殊動画の回帰テスト集を拡張する［A］

既存の合成動画試験に、編集済みスロー、音声開始ずれ、末尾破損などを追加する。 完了の目安：先頭・中間・末尾の実画像と期待フレーム番号を比較できる。

### 7. 動画照合の確実さを段階表示する［B］

既存の動画ハッシュ照合について、全体照合か限定的な照合かを区別する。 完了の目安：同名の別動画を誤って同一と見なさず、未確認も表示できる。

### 8. 表示桁数と内部座標精度を分離する［A］

座標の見せ方を変えても、取得した内部座標を不要に丸めない設計を検討する。 完了の目安：表示桁の変更・保存往復で内部座標が変わらない。

### 9. 取り消し履歴を一覧で確認できるようにする［B］

既存のUndo/Redoに、変更対象・件数・操作名の一覧を付ける。 完了の目安：戻す操作がどの点を変更するか事前に分かる。

### 10. 範囲外の座標を明示する［B］

トリミング範囲変更時に、範囲外へ隠れた座標の件数と保持状態を知らせる。 完了の目安：非表示と削除を混同せず、元の範囲へ戻して復元できる。

## 動作の軽さ

### 11. 体感速度を測る指標を設ける［A・第1段階完了］

既存の診断情報に、クリックから画像更新までの時間とP95を追加する。 完了の目安：改善前後を同じ端末・動画・操作手順で比較できる。

### 12. 画像キャッシュの総量を管理する［A・第1段階完了］

既存の枚数制限を、デジタイズ・分析・比較を含む総ピクセル量または推定バイト数でも制御する。 完了の目安：4K動画でタブを往復してもキャッシュが際限なく増えない。

### 13. 先読み方向を操作に合わせて変える［B］

既存の先読みに、移動方向・送り幅・空き予算を反映する。 完了の目安：逆方向へ切り替えたときに不要な先読みが優先されない。

### 14. 連続シークの要求をさらに整理する［A・第1段階完了］

既存の世代管理に加え、未開始の古い要求をまとめて減らす。 完了の目安：スライダーを連続操作した後、最後に指定した位置へ速く落ち着く。

### 15. 画面間の重複デコードを抑える［B］

同じ動画の同じフレームを複数画面が必要とするとき、共有可能な処理を調べる。 完了の目安：再利用によってデコード回数が減り、画面ごとの位置は独立を保つ。

### 16. 重い画面の初期化を遅らせる［B］

分析・比較などを初めて使うまで不要な準備を避ける。 完了の目安：初回表示が軽くなり、初めて開く画面の待ち時間も許容範囲に収まる。

### 17. 分析計算を画面操作から分離する［A・第1段階完了］

重い集計はWorker等に分け、古い座標リビジョンの結果を捨てる。 完了の目安：集計中でも打点・スクロールが止まらず、古い結果も表示しない。

### 18. 1点の修正で再計算する範囲を絞る［B］

座標更新時に、影響するマーカー・時間区間の集計だけ更新する。 完了の目安：全件計算と同じ結果を保ちながら更新時間を短縮できる。

### 19. 大きな座標表の描画量を制限する［B］

既存の表更新を、表示中の行・列だけ描く仕組みに発展させる。 完了の目安：大量マーカーでもコピー範囲を維持してスクロールできる。

### 20. 長時間利用の負荷試験を定期化する［A］

動画切替、ズーム、分析、保存を繰り返す連続試験を作る。 完了の目安：メモリ増加、フレーム遅延、残留Workerを検出できる。

## 操作画面の整理

### 21. よく使う設定だけを固定表示する［B］

細かな設定は既存の折りたたみに残し、頻用操作を選んで固定する。 完了の目安：設定欄を毎回探さず、動画領域も確保できる。

### 22. 動画と座標表の境界をドラッグ調整する［B］

既存のサイド幅設定に加え、動画と表の高さ配分を直接変えられるようにする。 完了の目安：最小サイズを守り、画面外へ操作欄が消えない。

### 23. 打点に集中する表示モードを作る［B］

詳細パネルを隠し、動画・現在点・フレーム・保存状態だけを残す。 完了の目安：通常表示へ戻しても配置や選択状態が維持される。

### 24. 小さい画面での配置を作業別に調整する［B］

横幅だけでなく画面の高さも見て、動画と操作欄の配分を変える。 完了の目安：小型ノートでも動画と主要操作を同時に確認できる。

### 25. マーカーの定義を参照できるようにする［B］

点名だけでなく、利用者が登録した解剖学的定義や注意を表示する。 完了の目安：同じ名前でも測定位置の解釈を共有できる。

### 26. 左右の取り違えを減らす表示を追加する［B］

色だけでなくR/Lや形を併用し、画面左右と身体左右を区別する。 完了の目安：左右の点を色覚に依存せず判別できる。

### 27. 危険な操作の確認を具体的にする［B］

削除・貼り付け・マーカー変更で、対象範囲と変更件数を表示する。 完了の目安：何が消えるかを確認してから実行できる。

### 28. ショートカットの競合を検査する［C］

既存のキー割当に重複検査と初期設定へ戻す操作を付ける。 完了の目安：入力欄やブラウザ標準操作を不用意に奪わない。

### 29. 打点失敗を視覚的に区別する［B］

受付済み・読み込み中で未受付・範囲外クリックを明確に分ける。 完了の目安：点が付かなかったときに連打せず原因を理解できる。

### 30. キーボードと読み上げで操作を完結させる［C］

フォーカス順、選択状態、進捗通知、操作対象の名前を見直す。 完了の目安：主要な作業をマウスだけに依存せず行える。

## 手動デジタイズの効率

### 31. 確認済みの点をロックする［B］

手入力・確認済みの点を、追跡や貼り付けから保護する。 完了の目安：ロック点を上書きするには明示的な解除が必要になる。

### 32. フレーム単位の確認済み状態を持つ［B］

点の入力完了と、画像を見て確認したことを分けて記録する。 完了の目安：入力率100%でも未確認のフレームを見分けられる。

### 33. 測定順序をプロトコルとして保存する［C］

既存のマーカーテンプレートと別に、打点順・左右順・作業範囲を保存する。 完了の目安：次マーカーの順序を研究手順に合わせて再現できる。

### 34. 貼り付け前に差分を表示する［B］

既存のExcel風貼り付けに、追加・上書き・無効値のプレビューを付ける。 完了の目安：意図しない列ずれや上書きを実行前に見つけられる。

### 35. 表の列幅と固定列を調整できるようにする［B］

フレーム列や選択マーカーを固定して、大きな表でも対応を見失わないようにする。 完了の目安：横スクロール中もフレームと点名が分かる。

### 36. 微調整量を研究条件に合わせて設定する［B］

既存の点移動キーに、小数ピクセル刻みなどの設定を追加する。 完了の目安：表示倍率が変わっても元画像上の移動量が一定になる。

### 37. 拡大中の画像端の操作を改善する［B］

拡大領域が画像端からはみ出す場面でも、クリック位置を把握しやすくする。 完了の目安：四隅でも拡大表示と保存座標が一致する。

### 38. イベントを基準に作業範囲を切り替える［C］

既存イベントから、接地前後などの短い打点区間へ移動できるようにする。 完了の目安：イベント位置を変えても区間の意味が保たれる。

### 39. 確認したい箇所のブックマークを付ける［B］

フレームとマーカーに短いメモを付け、後でまとめて確認する。 完了の目安：保存・再開後も対象点へ戻れる。

### 40. 手動点を隠した再測定モードを作る［B］

前回の打点を見せずに同じ箇所を測定し、後から比較する。 完了の目安：元データを変更せず、測定順と再測定結果を保存できる。

## 4点法・計測条件

### 41. 4点の対応を画面上で確認する手順を作る［A］

画像上P1〜P4と実座標欄を順に強調して、対応違いを減らす。 完了の目安：読込後に4点すべての対応を確認してから換算できる。

### 42. 不安定な4点配置を警告する［A］

単なる計算不能だけでなく、近接・ほぼ一直線・極端に細長い配置を検出する。 完了の目安：数値が出ても換算が不安定な条件を説明できる。

### 43. 初期の仮座標をそのまま使わせない工夫をする［A・第1段階完了］

既存の初期実座標について、実測値として確認した状態を保存する。 完了の目安：仮の1m枠のまま実長出力しようとしたときに気付ける。

### 44. 複数の独立検証点を登録できるようにする［B］

既存の単一検証点を、位置ごとの誤差分布を見られる形へ拡張する。 完了の目安：較正に使っていない複数点で誤差を確認できる。

### 45. 較正領域の内外を表示する［B］

4点が囲む範囲と対象点の位置関係を示し、外側の測定を区別する。 完了の目安：外挿域の点を自動削除せず、利用者に確認を促せる。

### 46. 較正点の打ち直しによる感度を調べる［C］

較正点を少し動かしたときの換算値の変化を試算する。 完了の目安：仮定した打点ずれと結果の変化をセットで表示できる。

### 47. カメラ移動による較正の無効化を検知する［B］

固定点の変化を検出し、較正を見直す候補区間を知らせる。 完了の目安：カメラが動いた動画で古い較正を無確認で使い続けない。

### 48. レンズ補正の入力を再利用しやすくする［B］

既存の内部パラメータ入力を、カメラ・解像度別の設定ファイルで管理する。 完了の目安：異なる解像度への誤適用を検出できる。

### 49. 単位変更時の扱いを明確にする［B］

単位名の付け替えと、数値をmからcmへ換算する操作を分ける。 完了の目安：単位だけ変わって数値が100倍ずれる事故を防げる。

### 50. 較正条件をまとめた記録を出力する［C］

4点の画像座標・実座標・単位・検証結果・補正設定を一つにまとめる。 完了の目安：結果のCSVと対応する較正条件を後から確認できる。

## 品質管理・測定の再現性

### 51. 点の由来と確認状態を別々に管理する［A］

既存の手入力・コピー・補間・AIという由来を、確認済みフラグと分ける。 完了の目安：確認してもAI由来の履歴が手入力扱いに書き換わらない。

### 52. 補間可能な欠測長を設定する［B］

既存の補間に、フレーム数だけでなく時間による上限を付ける。 完了の目安：長い遮蔽区間が無確認で補間されない。

### 53. 補間を適用する前後で品質を比較する［B］

既存プレビューに、補間件数・最大欠測長・速度変化を加える。 完了の目安：補間による改善と不自然な変化を同時に確認できる。

### 54. 急な座標移動の閾値を点ごとに設定する［B］

既存の異常移動候補を、点の種類・時刻差・較正単位に応じて調整する。 完了の目安：送り幅やFPSが異なっても同じ意味で評価できる。

### 55. 身体節の長さの変化を確認候補にする［C］

肩と肘などの距離変化を、誤打点の補助的な手掛かりとして表示する。 完了の目安：遠近変化を誤りと断定せず、確認候補として扱う。

### 56. 左右入れ替わり候補を連続区間で確認する［B］

既存の左右交差チェックを、前後の点列を含むレビューへ発展させる。 完了の目安：実際の交差と誤ったラベル交換を人が区別できる。

### 57. 再測定比較をマーカー別・軸別にする［A・第1段階完了］

既存の比較統計で、xとyや異なる関節をまとめた結果だけに依存しないようにする。 完了の目安：一致したフレーム・点だけで比較し、集計単位を明示できる。

### 58. 再測定差の分布図を追加する［C］

既存の平均差等に加え、系統的なずれとばらつきを可視化する。 完了の目安：欠測と対応付け条件を明記して差を評価できる。

### 59. 確認待ちの点を優先順に並べる［B］

既存の要確認ジャンプを、重大な時刻問題・大きなずれ・低信頼候補の順にする。 完了の目安：多数の候補から優先して見るべき箇所を選べる。

### 60. 出力に利用者の確認状況を付ける［A］

既存の出力前チェックを、未確認項目と許容した理由まで記録する。 完了の目安：チェック実行済みを品質保証済みと誤解しない。

## 分析・現場用ステップ

### 61. 平滑化前後を同じグラフで比較する［B］

既存の移動平均に、元データと処理後の比較表示を付ける。 完了の目安：原座標を変えずに処理の影響を確認できる。

### 62. 速度・加速度を計算できない条件を表示する［B］

欠測・時刻重複・端点などで計算不可になった理由を示す。 完了の目安：値が空欄なのかゼロなのかを区別できる。

### 63. 角度の定義を図で確認できるようにする［B］

既存の角度テンプレートに、3点の順序と角度の方向を表示する。 完了の目安：同じ名称でも異なる角度定義を取り違えない。

### 64. イベント基準の時間軸で試技を比較する［C］

既存の試技一覧とイベントを使い、接地を0秒にそろえて表示する。 完了の目安：元時刻を残しながら揃えた基準を追跡できる。

### 65. 動作周期を百分率にそろえて比較する［C］

任意の開始・終了イベント間を0〜100%に正規化する。 完了の目安：時間正規化による補間方法を明記できる。

### 66. 比較動画を複数イベントで同期する［B］

既存の固定フレームオフセットに、イベント対による時刻合わせを追加する。 完了の目安：異なるFPSでも対応付けの誤差を確認できる。

### 67. 接地・離地時刻の不確かさを示す［A］

単一フレームの確定値だけでなく、利用者が確認した候補区間を保持する。 完了の目安：時間分解能を超えた精度を装わず結果を出せる。

### 68. 対象人物の追跡が入れ替わった区間をレビューする［B］

既存の対象者選択に、途中の人物同一性を確認する機能を付ける。 完了の目安：人が交差する動画で別人物の結果を混ぜない。

### 69. 実長がない状態の歩幅出力を明確に制限する［A］

ステップ時間と実長指標で必要な条件を別々に確認する。 完了の目安：較正不足でも出せる値と出せない値を区別できる。

### 70. 現場条件別の検証用データセットを作る［A］

衣服・照明・速度・遮蔽等の条件を分け、手動確認値と比較する。 完了の目安：良好例だけでなく失敗条件と誤差も公開判断に使える。

## 保存・データ受け渡し

### 71. 複数世代の復旧データを保持する［A］

既存の自動保存に、容量上限付きの少数世代を追加する。 完了の目安：誤操作後に最新データ以外へ戻れ、動画本体は保存しない。

### 72. 保存済み状態を保存先別に表示する［B］

手動ファイル、端末自動保存、任意のクラウド同期を分けて知らせる。 完了の目安：どこまで書き込みが完了したかを区別できる。

### 73. 同一プロジェクトの複数タブ編集を検知する［A・第1段階完了］

同じ保存対象を開いたときに、競合や別コピーへの分岐を案内する。 完了の目安：後から閉じたタブで新しい座標が消える事故を防げる。

### 74. 保存処理の失敗試験を増やす［A・第1段階完了］

容量不足、権限失効、中断、途中書込の失敗を試験する。 完了の目安：保存失敗を成功と表示せず、既存の有効ファイルを守れる。

### 75. 旧形式プロジェクトの移行試験を継続する［A］

既存のスキーマ対応に、実際の旧保存例を使った往復試験を追加する。 完了の目安：点、欠測理由、時刻、較正、表示設定の欠落を検出できる。

### 76. 表のインポートを対応付けプレビュー付きにする［B］

既存のCSV・TSV読込で、列名・点名・単位・フレーム番号を確認する。 完了の目安：似た点名や列順の違いを明示的に解決できる。

### 77. 解析用CSVの列定義書を同時に出す［B］

列名、単位、欠測表現、座標系、時刻基準を機械可読と人向けに整理する。 完了の目安：別の研究者が推測せずに読み込める。

### 78. Excel向けと解析コード向けのCSVを区別する［B］

文字コード、区切り、数値形式、危険な文字列の扱いを用途別に明示する。 完了の目安：表計算の数式実行を防ぎつつ座標値を破壊しない。

### 79. 派生結果の再計算条件を保存する［C］

既存の分析プロトコルに、アプリ版数・実装版・計算条件を結びつける。 完了の目安：同じ入力から同じ処理条件で結果を再生成できる。

### 80. 配布用プロジェクトの匿名化プレビューを作る［B］

共有前にファイル名・メモ・試技情報などを点検し、削除候補を選ぶ。 完了の目安：動画を含めず、匿名化後も分析に必要な情報が残る。

## ガイド・利用支援

### 81. 各設定から該当ガイドへ直接移動する［A・第1段階完了］

既存のガイドの見出しへ、設定ごとの小さなヘルプリンクを付ける。 完了の目安：今触っている機能の説明をすぐ開ける。

### 82. 操作を試せる練習用プロジェクトを用意する［B］

個人を特定できないサンプル動画・座標・4点法データを用意する。 完了の目安：配布権利を確認し、元の作業を壊さず練習できる。

### 83. 4点法の正しい例と間違い例を増やす［B］

対応順違い、単位違い、ほぼ一直線の配置を図と結果で示す。 完了の目安：自分の設定のどこを直すべきか判断できる。

### 84. エラー文と解決手順を対応付ける［B］

既存のエラー表示に、理由別のガイドへの入口を付ける。 完了の目安：再読込で直る問題と入力動画の問題を混同しない。

### 85. ガイド検索の言い換えを増やす［B］

LLMを使わず、コマ送り・フレーム移動などの同義語を対応付ける。 完了の目安：専門用語を知らなくても目的の項目が見つかる。

### 86. 初心者向けと研究者向けの説明を分ける［C］

操作手順を短く見せ、数式・座標定義・制約は詳しい説明へ分ける。 完了の目安：初心者の負担を増やさず条件を省略しない。

### 87. 画面とガイドの差分を自動検査する［B］

ボタン名、ID、見出し、画面キャプチャを更新時に確認する。 完了の目安：存在しないボタンを案内する文書の公開を防ぐ。

### 88. 変更点を利用者の作業に結びつけて案内する［C］

既存の更新確認に、何が変わり再確認が必要かを短く表示する。 完了の目安：通常の改善と測定結果へ影響する変更を区別できる。

### 89. 匿名の診断情報を選んで出せるようにする［B］

既存の診断出力に、ファイル名・識別情報を除いた初期設定と内容確認を追加する。 完了の目安：送信前に利用者自身が含まれる情報を確認できる。

### 90. 研究室向けの導入チェックリストを作る［C］

対応動画の確認、保存練習、4点法の検証、復旧テストを手順化する。 完了の目安：本番の被験者データを扱う前に環境を確認できる。

## 安全性・保守・配布

### 91. ローカルAPIの攻撃試験を更新に組み込む［A］

既存のトークン・Origin・Host検証を、異常な要求でも試験する。 完了の目安：外部ページからの終了・動画変更などが拒否される。

### 92. 入力ファイルの悪意ある内容を試験する［A］

巨大JSON、ZIP展開量、異常な数値、CSV数式、HTML文字列などの処理を確認する。 完了の目安：画面固着・コード実行・メモリ枯渇を防げる。

### 93. 依存ライブラリとライセンス一覧を自動生成する［B］

実際に配布する版・機能・モデルに対応した一覧を作る。 完了の目安：削除済みの依存や追加したモデルの通知漏れを検出できる。

### 94. 公開前に秘密情報と私的データを検査する［A］

認証情報、個人動画、ローカルパス、実験結果が配布物へ混ざらないか検査する。 完了の目安：テスト用ファイルを含めて公開対象を点検できる。

### 95. 更新時の版の混在を検出する［B］

既存のキャッシュ更新に、HTML・JS・データ定義が対応しているかの確認を追加する。 完了の目安：古いタブでも未保存作業を守り、更新の必要性を知らせる。

### 96. 不具合発生時に戻せる公開手順を整える［B］

静的Web版とアプリ版の既知の正常版を特定し、戻す手順を試験する。 完了の目安：前版へ戻しても新しい保存形式を壊さない判断ができる。

### 97. 大きい処理の責務を段階的に分離する［A］

app.jsの保存・較正・座標・表示などを、挙動を固定するテストとともに分ける。 完了の目安：分割だけで動作が変わらず、修正の影響範囲が小さくなる。

### 98. ブラウザ別の受入試験を定義する［B］

既存のChrome・WebKit試験に、対応を表明する環境ごとの操作項目を揃える。 完了の目安：ブラウザ名だけでなく実施版・動画条件を記録できる。

### 99. 利用者が選ぶ機能構成を作る［B］

手動デジタイズ中心なら追跡・ステップ分析を隠すなど、初期画面を目的別にする。 完了の目安：使わない機能を読み込まず、必要時に戻せる。

### 100. 改善の採否を実利用者の作業で決める［A］

代表的な作業の所要時間、誤操作、復旧成功率を実装前後で比較する。 完了の目安：機能数ではなく、軽さと測定の確実さが改善したか判断できる。
